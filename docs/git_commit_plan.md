# Suggested Git History

After configuring your real Git name/email, create commits in this order if you want a clean recruiter-facing history:

1. `initialize FastAPI project structure`
2. `add procurement data model and synthetic seed data`
3. `implement policy ingestion and retrieval pipeline`
4. `add safe SQL query tool`
5. `add LangGraph routing and hybrid workflows`
6. `add supplier risk and escalation reporting`
7. `add Streamlit client and document endpoints`
8. `add evaluation and safety tests`
9. `add Docker and CI workflow`
10. `finalize documentation and interview guide`

Do not rewrite dates or fake authorship. Use your real Git identity.
